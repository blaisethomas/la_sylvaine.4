<%= form_for @pupil_has_year, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :adjusted_price, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :adjusted_price, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  pupil_has_years_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
