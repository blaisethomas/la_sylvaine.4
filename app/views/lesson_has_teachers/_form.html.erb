<%= simple_form_for(@lesson_has_teacher) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :lesson %>
    <%= f.association :teacher %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
